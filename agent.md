# Agent Instructions

- Do not modify `train_vsi.sh` unless the user specifically asks for changes to that file.
- Prefer adding reusable code/config options in the Python implementation or dedicated experiment wrappers instead of changing `train_vsi.sh` by default.
